@model MVC_StokTakip.Models.Entity.Kullanici1

@{
    ViewBag.Title = "Login";
}

<h1>Giriş</h1>

<div class="container">
    @using (Html.BeginForm("Login", "Kullanici", FormMethod.Post))
    {
        <div class="form-group">
            @Html.LabelFor(x => x.KullaniciAdi)
            @Html.TextBoxFor(x => x.KullaniciAdi, new { @class = "form-control" })
        </div>
        @*<div class="form-group">
                @Html.LabelFor(x => x.Sifre)
                @Html.TextBoxFor(x => x.Sifre, new { @class = "form-control" })
            </div>*@
        <div class="form-group">
            <label>Şifre</label>
            <input class="form-control" type="password" name="Sifre" />
        </div>
        @*<div class="form-group">
            @Html.LabelFor(x => x.BeniHatırla)
            @Html.CheckBoxFor(x => x.BeniHatırla, new { @class = "form-control" })
        </div>*@
        <div>
            <div><button class="btn btn-success">Giriş</button></div>
            <div class="container">
                @if (ViewBag.hata != null)
                {
                    <p>@ViewBag.hata</p>
                }
            </div>
        </div>
    }
</div>
    
